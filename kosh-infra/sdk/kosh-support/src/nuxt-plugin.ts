// Nuxt plugin for registering Kosh Support components
// Usage in nuxt.config.ts:
// plugins: ['@kosh/support/dist/nuxt-plugin']

export default (nuxtApp: any) => {
  console.log('[Kosh Support] Nuxt plugin loaded')
  // Components are dynamically loaded
}
